% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotBS.R
\name{dyBS}
\alias{dyBS}
\title{Brier Scores for Terminal Survival Prediction}
\usage{
dyBS(surv, S_D, T2, event2, times=knots(S_D), reference = TRUE,
  tu=NULL, int.method=c("none","fmm","natural","periodic","monoH.FC","hyman"))
}
\arguments{
\item{surv}{a list containing \code{surv} (survival prediction output from object of class "mscr") and \code{t1} (available observations for multiple nonterminal event times).}

\item{S_D}{(estimated or specified) survival function for the terminal event time. See Examples.}

\item{T2}{A numeric vector of observed terminal event times. Its length must
equal the number of rows of \code{event2}.}

\item{event2}{A numeric or logical vector of censoring indicators for the
terminal event time, where 1 indicates the terminal event is observed and
0 indicates censoring.}

\item{times}{evaluation times for the Brier score curve}

\item{reference}{If TRUE, the returned object is a list containing the relative Brier Score compared to the predicted survival curve yielded from the benchmark Kaplan Meier estimator.}

\item{tu}{A numeric value specifying the maximum time point for the Brier score curve. If \code{tu = NULL}, no maximum time point is used.}

\item{int.method}{specifies the type of spline to be used.
Possible values are "none","fmm", "natural", "periodic", "monoH.FC" and "hyman". 
See also \code{\link[stats]{splinefun}}.}
}
\value{
A list containing the following components: BS=data.frame(time=times,BS=BS), IBS=IBS
\describe{
    \item{BS}{Estimated Brier Score Curve.}
    \item{IBS}{Integrated Brier Score over the range of evaluation times.}
  }
}
\description{
Brier Scores for Terminal Survival Prediction
}
\examples{
\donttest{
set.seed(12345)
data<- simSCRmul(n=100,copulafam = "frank",K=3,
                 tau.alpha = .5,tau.theta=.5)

fit<- mscr(mT1=data$T1,T2=data$T2,mevent1=data$event1,event2=data$event2,
           copulafam = "frank")
mT1.te<- data$T1
mT1.te[data$event1==0]<- NA

####  dynamic prediction
surv<- predict(fit, t1obs=mT1.te,type="surv")

## naive Pr(D>t|D> t_m)
surv0<- predict(fit, t1obs=mT1.te,type="surv",cause=0)

## Pr(D>t|T_1=t_1,D> t_1)
surv1<- predict(fit, t1obs=mT1.te,type="surv",cause=1,maxobs = FALSE)

## Pr(D>t|T_1=t_1,D> t_m)
surv1m<- predict(fit, t1obs=mT1.te,type="surv",cause=1,maxobs = TRUE)

### Brier Scores
S_D<- terminal_survival(fit)
BS<- dyBS(surv,S_D = S_D,T2=data$T2,event2=data$event2,int.method = "natural")
BS0<- dyBS(surv0,S_D = S_D,T2=data$T2,event2=data$event2,int.method = "natural")
BS1<- dyBS(surv1,S_D = S_D,T2=data$T2,event2=data$event2,int.method = "natural")
BS1m<- dyBS(surv1m,S_D = S_D,T2=data$T2,event2=data$event2,int.method = "natural")
ibs<-c(integrated_brier_score(BS),
  integrated_brier_score(BS0),
  integrated_brier_score(BS1),
  integrated_brier_score(BS1m)) ## integrated BS

### plot Brier Scores
plot(BS$BS,ylim = c(0,0.7),type="l",xlab="time",ylab="Brier Score",col=1)
lines(BS0$BS,col=2)
lines(BS1$BS,col=3)
lines(BS1m$BS,col=4)
legend("topright",col = 1:4,lty = 1,legend = c("DP","P0","P1","P1m"))
}
}
\seealso{
\code{\link{mscr}}, \code{\link{predict.mscr}}
}
